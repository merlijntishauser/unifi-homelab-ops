services:
  api:
    build: ./backend
    ports:
      - "8001:8000"
    volumes:
      - ./backend/app:/app/app
      - ./backend/alembic:/app/alembic
      - ./backend/tests:/app/tests
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - SUPPRESS_HEALTHCHECK_ACCESS_LOGS=true
    healthcheck:
      # 127.0.0.1, not localhost: the app listens on IPv4 only, and localhost
      # resolves to ::1 first. urllib falls back to IPv4, but a client that
      # does not (busybox wget) would report connection refused here.
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health')"]
      interval: 2s
      timeout: 3s
      retries: 15
      start_period: 5s

  frontend:
    build: ./frontend
    ports:
      - "5174:5173"
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/index.html:/app/index.html
      - ./frontend/vite.config.ts:/app/vite.config.ts
    environment:
      - API_PROXY_TARGET=http://api:8000
    depends_on:
      api:
        condition: service_healthy
